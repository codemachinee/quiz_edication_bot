# Образовательный Квиз-Бот

[![Python](https://img.shields.io/badge/Python-3.12-blue.svg)](https://www.python.org/downloads/release/python-3120/)
[![Docker](https://img.shields.io/badge/Docker-gray.svg?logo=docker)](https://www.docker.com/)

Телеграм-бот для проведения образовательных викторин. Проект написан на Python с использованием асинхронной библиотеки и конечных автоматов (FSM) для управления состоянием диалога.

## 🚀 Технологии

- **Python 3.12**
- **Poetry & UV**: Для управления зависимостями и окружением.
- **Docker**: Для контейнеризации приложения.
- **Aiogram (предположительно)**: Как основа для работы с Telegram Bot API.

## 🐳 Установка и запуск с помощью Docker

Вы можете легко развернуть бота, используя Docker.

### 1. Клонируйте репозиторий

```shell
git clone <URL вашего репозитория>
cd <имя папки проекта>
```

### 2. Создайте файл окружения

В корневой директории проекта создайте файл с именем `.env`. Он необходим для хранения секретных данных, таких как токен вашего бота.

Скопируйте в него следующее содержимое и замените `<ВАШ_ТЕЛЕГРАМ_ТОКЕН>` на реальный токен, полученный от [@BotFather](https://t.me/BotFather).

```env
# Токен вашего Telegram-бота
TOKEN=<ВАШ_ТЕЛЕГРАМ_ТОКЕН>

# Другие переменные окружения (если они вам нужны)
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id
```

### 3. Соберите Docker-образ

Выполните команду в терминале, находясь в корневой папке проекта:

```shell
docker build -t quiz-education-bot .
```
Эта команда соберёт образ с именем `quiz-education-bot` на основе `Dockerfile`.

### 4. Запустите контейнер

Теперь запустите контейнер из созданного образа:

```shell
docker run --env-file .env --name quiz-bot -d quiz-education-bot
```
- `docker run`: команда для запуска контейнера.
- `--env-file .env`: передает переменные из файла `.env` внутрь контейнера.
- `--name quiz-bot`: задает имя контейнера для удобного управления.
- `-d`: запускает контейнер в фоновом режиме (detached).
- `quiz-education-bot`: имя образа, который мы собрали на предыдущем шаге.

### Управление контейнером

- **Посмотреть логи:**
  ```shell
  docker logs quiz-bot
  ```
- **Остановить контейнер:**
  ```shell
  docker stop quiz-bot
  ```
- **Удалить контейнер:**
  ```shell
  docker rm quiz-bot
  ```

После выполнения этих шагов ваш бот будет запущен и готов к работе в Telegram.
